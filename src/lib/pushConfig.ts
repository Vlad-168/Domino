// Web Push (VAPID) public key for FCM background notifications.
//
// Get it in Firebase Console → Project settings → Cloud Messaging →
// "Web Push certificates" → "Key pair" (generate one if empty), then paste
// the key string here. Until it is filled in, the app falls back to in-app
// notifications only (no push when the app is fully closed).
export const VAPID_KEY = ''
